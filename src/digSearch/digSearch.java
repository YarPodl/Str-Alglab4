package digSearch;

import java.util.HashMap;
import java.util.Map;

public class digSearch {


    /*
    private Map<Character, Map> tree;

    public digSearch() {
        tree = new HashMap<>();
    }

    public String add(String value){
        if (!value.isEmpty()){
            return "Пустое слово";
        }

        for (int i = 0; i < value.length(); i++) {
            Map current = tree.get(value.charAt(i));
            if (current == null){
                tree.put(value.charAt(i), new HashMap());
            }
            tree.put(value.charAt(i), );
        }
    }

    */



}
